{
  "name": "checkpoint6",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm run setup:server",
    "setup:app": "pnpm run setup:server && pnpm run setup:client",
    "setup:client": "cd client && pnpm i",
    "setup:server": "cd server && pnpm i",
    "start": "node server/index.js"
  }
}
